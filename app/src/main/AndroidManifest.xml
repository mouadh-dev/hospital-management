<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.stagepfe">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.NoActionBar">
        <activity android:name=".Patient.BottomBarActionPatientActivity"></activity>
        <activity android:name=".Patient.ReclamationActivity" />
        <activity
            android:name=".Patient.AccountPatientActivity"
            android:label="@string/title_activity_account_patient" />
        <activity android:name=".Doctor.Activity.DoctorProfilActivity" />
        <activity android:name=".Doctor.Activity.AccountDoctorActivity" />
        <activity android:name=".Authentication.Activity.ContainerFragmentPasswordActivity" />
        <activity android:name=".Authentication.Activity.AuthenticationFragmentContainerActivity" />
        <activity android:name=".Doctor.Activity.DoctorOptionsActivity" />
        <activity android:name=".Authentication.Activity.MainPageActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>