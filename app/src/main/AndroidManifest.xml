<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.stagepfe">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.NoActionBar">
        <activity android:name=".Activity.Patients.BottomBarActivity"></activity>
        <activity android:name=".Activity.Patients.ProfilePatientActivity" />
        <activity android:name=".Patient.Activity.MessagePatientActivity" />
        <activity android:name=".Patient.Activity.NotificationPatientActivity" />
        <activity android:name=".Patient.Activity.ReclamationPatientActivity" />
        <activity
            android:name=".Patient.Activity.AccountPatientActivity"
            android:label="@string/title_activity_account_patient" />
        <activity android:name=".Activity.Doctors.DoctorProfilActivity" />


        <activity android:name=".MainPageActivity"/>
            <activity android:name=".Activity.Authentication.ContainerFragmentPasswordActivity"/>
                <activity android:name=".Activity.Authentication.AuthenticationFragmentContainerActivity"/>

        <activity android:name=".Activity.Doctors.AccountDoctorActivity">

                <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>